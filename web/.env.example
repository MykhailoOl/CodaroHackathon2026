# Origin of the Spring Boot API. The app proxies /api, /login and /logout under
# this origin via next.config.mjs so the session cookie and CSRF token travel
# same-origin — no CORS, no bearer tokens.
API_ORIGIN=http://localhost:8080